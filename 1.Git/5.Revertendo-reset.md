# Revertendo com reset

Diferente do revert, o reset ao invés de criar um commit e voltar o arquivo para o estado anterior, ele só volta para o estado anterior. Pode ser acompanhado de 2 tipos de parâmetros:
- hard: volta para o commit anterior ignorando tudo que foi feito;
- soft: deixa as modificações mas apaga o commit defeituoso. 